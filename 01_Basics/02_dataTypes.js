"use struct"; //Treats all JS code as newer version.


// DataTypes :- numbers, bigInt, string, boolean, null ->Standlone value(representation of empty value), 
// undefined, symbol ->unique( most used in React) => primitive dt

// object :-
console.log(typeof(undefined));
console.log(typeof(null));
